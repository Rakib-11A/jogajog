<nav class="bg-primary text-gray-700 px-6 py-4 shadow">
  <div class="container mx-auto flex justify-between items-center">
    <h1 class="text-2xl font-bold">যোগাযোগ</h1>
    <% if user_signed_in? %>
      <div class="flex gap-4 items-center">
        <%= link_to "চ্যাটরুম", chatrooms_path, class: "hover:underline" %>
        <%= link_to "প্রোফাইল", user_path(current_user), class: "hover:underline" %>
        <%= link_to "লগ আউট", destroy_user_session_path, method: :delete, class: "hover:underline" %>
      </div>
    <% else %>
      <%= link_to "লগইন", new_user_session_path, class: "hover:underline" %>
    <% end %>
  </div>
</nav>